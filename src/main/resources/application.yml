spring:
  datasource:
    url: jdbc:mysql://localhost:3306/forum?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 970614
    driver-class-name: com.mysql.jdbc.Driver
  thymeleaf:
    mode: HTML
    encoding: UTF-8
    servlet:
      content-type: text/html
    cache: false


mybatis:
  type-aliases-package: com.ftt.forum.entity

#showSql
logging:
  level:
    com:
      ftt:
        forum:
          mapper: debug

server:
  port: 8080

